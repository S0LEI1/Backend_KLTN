import { Subjects } from '../subjects';
export interface AccountDeletedEvent {
    subject: Subjects.AccountDeleted;
    data: {
        id: string;
        version: number;
        user: string;
    };
}
