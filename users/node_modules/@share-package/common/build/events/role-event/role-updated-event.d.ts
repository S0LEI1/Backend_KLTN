import { Subjects } from '../subjects';
export interface RoleUpdatedEvent {
    subject: Subjects.RoleUpdated;
    data: {
        id: string;
        name: string;
        active: boolean;
        description: string;
        version: number;
    };
}
