import { Subjects } from '../subjects';
export interface CustomerUpdatedEvent {
    subject: Subjects.CustomerUpdated;
    data: {
        id: string;
        fullName: string;
        gender: boolean;
        avatar?: string;
        address: string;
        version: number;
    };
}
