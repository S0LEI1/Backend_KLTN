"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.singleUploadMiddleware = exports.multipleUploadMiddleware = void 0;
const util_1 = __importDefault(require("util"));
const multer_1 = __importDefault(require("multer"));
let TYPE_MATCH = [
    'image/png',
    'image/jpeg',
    'image/gif',
    'video/mp3',
    'video/mp4',
    'application/pdf',
    'application/msword',
    'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
    'application/vnd.ms-powerpoint',
    'application/vnd.openxmlformats-officedocument.presentationml.presentation',
    'application/vnd.rar',
    'application/zip',
];
const FILE_SIZE = 20971520;
let storage = multer_1.default.diskStorage({
    filename: (req, file, callback) => {
        const splitTempt = file.originalname.split('.');
        const fileExtension = splitTempt[splitTempt.length - 1];
        if (fileExtension === 'rar') {
            file.mimetype = 'application/vnd.rar';
        }
        if (fileExtension === 'zip') {
            file.mimetype = 'application/zip';
        }
        if (TYPE_MATCH.indexOf(file.mimetype) === -1) {
            let errorMess = new Error(`The file <strong>${file.originalname}</strong> is invalid. Only allowed to upload image jpeg or png.`);
            return callback(errorMess, '');
        }
        let filename = `${Date.now()}-spa-${file.originalname}`;
        callback(null, filename);
    },
});
let uploadManyFiles = (0, multer_1.default)({
    storage,
    limits: { fileSize: FILE_SIZE },
}).array('files', 10);
let uploadFile = (0, multer_1.default)({ storage, limits: { fileSize: FILE_SIZE } }).single('file');
exports.multipleUploadMiddleware = util_1.default.promisify(uploadManyFiles);
exports.singleUploadMiddleware = util_1.default.promisify(uploadFile);
