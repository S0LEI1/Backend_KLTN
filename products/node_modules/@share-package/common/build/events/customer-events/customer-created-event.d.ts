import { Subjects } from '../subjects';
export interface CustomerCreatedEvent {
    subject: Subjects.CustomerCreated;
    data: {
        id: string;
        fullName: string;
        phoneNumber: string;
        gender: boolean;
        address: string;
        avatar?: string;
        email: string;
        password: string;
    };
}
