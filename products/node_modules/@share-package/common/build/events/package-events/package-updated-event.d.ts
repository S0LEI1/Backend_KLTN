import { Subjects } from '../subjects';
export interface PackageUpdatedEvent {
    subject: Subjects.PackageUpdated;
    data: {
        id: string;
        name: string;
        salePrice: number;
        imageUrls: string[];
        description: string;
        featured?: boolean;
        discount?: boolean;
        active?: boolean;
        version: number;
    };
}
