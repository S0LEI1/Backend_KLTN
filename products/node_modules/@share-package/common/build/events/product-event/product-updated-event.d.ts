import { Subjects } from '../subjects';
export interface ProductUpdatedEvent {
    subjects: Subjects.ProductUpdated;
    data: {
        id: string;
        name: string;
        imageUrl: string;
        description: string;
        suplier: string;
        version: number;
        active: boolean;
    };
}
