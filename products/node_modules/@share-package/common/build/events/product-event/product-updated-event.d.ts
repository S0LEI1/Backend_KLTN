import { Subjects } from '../subjects';
export interface ProductUpdatedEvent {
    subject: Subjects.ProductUpdated;
    data: {
        id: string;
        name: string;
        imageUrl: string;
        description: string;
        suplierId: string;
        categoryId: string;
        version: number;
        active: boolean;
    };
}
