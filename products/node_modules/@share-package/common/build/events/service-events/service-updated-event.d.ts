import { Subjects } from '../subjects';
export interface ServiceUpdatedEvent {
    subject: Subjects.ServiceUpdated;
    data: {
        id: string;
        name: string;
        imageUrl: string;
        salePrice: number;
        active?: boolean;
        discount?: number;
        description: string;
        version: number;
    };
}
