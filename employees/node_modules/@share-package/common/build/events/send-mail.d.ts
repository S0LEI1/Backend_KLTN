interface mailPayload {
    authMail: string;
    authPassword: string;
    toEmail: string;
    otp: string;
    message: string;
}
export declare const SendMail: (payload: mailPayload) => Promise<import("nodemailer/lib/smtp-transport").SentMessageInfo>;
export {};
