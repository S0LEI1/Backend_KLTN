import { Subjects } from '../subjects';
export interface PermissionUpdatedEvent {
    subject: Subjects.PermissionUpdated;
    data: {
        id: string;
        name: string;
        systemName: string;
        description: string;
        version: number;
    };
}
