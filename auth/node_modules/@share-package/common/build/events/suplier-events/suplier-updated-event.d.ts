import { Subjects } from '../subjects';
export interface SuplierUpdatedEvent {
    subject: Subjects.SuplierUpdated;
    data: {
        id: string;
        name: string;
        phoneNumber: string;
        email: string;
        address: string;
        description: string;
        version: number;
    };
}
