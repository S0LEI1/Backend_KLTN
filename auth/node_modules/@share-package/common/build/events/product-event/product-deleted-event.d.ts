import { Subjects } from '../subjects';
export interface ProductDeletedEvent {
    subjects: Subjects.ProductDeleted;
    data: {
        id: string;
        version: number;
        active: boolean;
    };
}
