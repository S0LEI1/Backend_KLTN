import { Subjects } from '../subjects';
import { ShiftStatus } from '../types/shift-status';
export interface UserShiftCreatedEvent {
    subject: Subjects.UserShiftCreated;
    data: {
        id: string;
        userId: string;
        shiftId: string;
        status: ShiftStatus;
    };
}
