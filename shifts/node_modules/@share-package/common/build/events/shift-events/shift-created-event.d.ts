import { Subjects } from '../subjects';
export interface ShiftCreatedEvent {
    subject: Subjects.ShiftCreated;
    data: {
        id: string;
        begin: Date;
        end: Date;
        description: string;
    };
}
