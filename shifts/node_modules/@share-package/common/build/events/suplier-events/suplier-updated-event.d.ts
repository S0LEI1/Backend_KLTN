import { Subjects } from '../subjects';
export interface SuplierUpdatedEvent {
    subject: Subjects.SuplierUpdated;
    data: {
        id: string;
        name: string;
        description: string;
        version: number;
    };
}
