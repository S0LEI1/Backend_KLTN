"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Pagination = void 0;
class Pagination {
    static query() {
        const query = {};
        return query;
    }
    static options(pages, limit, sortBy) {
        const otps = {
            skip: (parseInt(pages) - 1) * parseInt(limit, 10),
            limit: parseInt(limit, 10),
            sort: sortBy,
        };
        return otps;
    }
}
exports.Pagination = Pagination;
