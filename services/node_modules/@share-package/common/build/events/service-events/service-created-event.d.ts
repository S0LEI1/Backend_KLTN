import { Subjects } from '../subjects';
export interface ServiceCreatedEvent {
    subject: Subjects.ServiceCreated;
    data: {
        id: string;
        name: string;
        imageUrl: string;
        price: number;
        active: boolean;
        description: string;
    };
}
