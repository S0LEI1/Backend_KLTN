import { Subjects } from '../subjects';
export interface ServiceUpdatedEvent {
    subject: Subjects.ServiceUpdated;
    data: {
        id: string;
        name: string;
        imageUrl: string;
        price: number;
        active: boolean;
        description: string;
        version: number;
    };
}
