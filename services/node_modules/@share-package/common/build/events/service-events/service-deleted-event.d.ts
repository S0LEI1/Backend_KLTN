import { Subjects } from '../subjects';
export interface ServiceDeletedEvent {
    subject: Subjects.ServiceDeleted;
    data: {
        id: string;
        active: boolean;
        version: number;
    };
}
