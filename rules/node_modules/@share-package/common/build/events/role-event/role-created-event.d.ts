import { Subjects } from '../subjects';
export interface RoleCreatedEvent {
    subject: Subjects.RoleCreated;
    data: {
        id: string;
        name: string;
        active: boolean;
        description: string;
        version: number;
    };
}
