import { Subjects } from '../subjects';
export interface UserURMappingCreatedEvent {
    subject: Subjects.UserURMCreated;
    data: {
        id: string;
        userId: string;
        roleId: string;
        version: number;
    };
}
