import { Subjects } from '../subjects';
export interface UserCreatedEvent {
    subject: Subjects.UserCreated;
    data: {
        id: string;
        fullName: string;
        gender: boolean;
        avatar?: string;
        version: number;
    };
}
