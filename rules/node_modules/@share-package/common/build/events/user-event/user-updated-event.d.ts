import { Subjects } from '../subjects';
export interface UserUpdatedEvent {
    subject: Subjects.UserCreated;
    data: {
        id: string;
        fullName: string;
        gender: boolean;
        avatar?: string;
        version: number;
    };
}
