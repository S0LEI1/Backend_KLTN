import { Subjects } from '../subjects';
export interface AccountRoleCreatedEvent {
    subject: Subjects.AccountRoleCreated;
    data: {
        id: string;
        accountId: string;
        roleId: string;
    };
}
