import { Subjects } from '../subjects';
export interface ProductCreatedEvent {
    subjects: Subjects.ProductCreated;
    data: {
        id: string;
        name: string;
        imageUrl: string;
        description: string;
        suplier: string;
    };
}
