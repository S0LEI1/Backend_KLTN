import { Subjects } from '../subjects';
export interface RoleDeletedEvent {
    subject: Subjects.RoleDeleted;
    data: {
        id: string;
        active: boolean;
        version: number;
    };
}
