import { Subjects } from '../subjects';
export interface SuplierCreatedEvent {
    subject: Subjects.SuplierCreated;
    data: {
        id: string;
        name: string;
        description: string;
    };
}
